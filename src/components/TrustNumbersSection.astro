---
import { trustNumbers, trustStatement } from '../content.config';

const visible = trustNumbers.filter((item) => item.publish);
---

{visible.length > 0 && (
  <section class="band band-alt border-t border-line py-14 md:py-20" aria-labelledby="trust-title">
    <div class="shell">
      <h2 id="trust-title" class="display-3 max-w-2xl text-body">
        Belajar dari ekosistem bengkel yang benar-benar beroperasi.
      </h2>

      <dl class="mt-10 grid gap-px border border-line bg-line sm:grid-cols-3">
        {visible.map((item) => (
          <div class="bg-ground px-6 py-8">
            <dt class="sr-only">{item.label}</dt>
            <dd>
              <span class="block font-display text-5xl font-bold leading-none text-accent md:text-6xl">{item.value}</span>
              <span class="mt-3 block text-sm leading-relaxed text-muted">{item.label}</span>
            </dd>
          </div>
        ))}
      </dl>

      <p class="lead mt-10 max-w-prose text-muted">{trustStatement}</p>
    </div>
  </section>
)}
