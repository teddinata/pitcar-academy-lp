---
import { problems, problemResolution } from '../content.config';
---

<section class="band border-t border-line" aria-labelledby="problem-title">
  <div class="shell">
    <div class="max-w-3xl">
      <p class="eyebrow text-accent">Masalahnya</p>
      <h2 id="problem-title" class="display-2 mt-6">Kenapa belajar teori saja tidak cukup?</h2>
    </div>

    <ol class="mt-14 grid gap-px border border-line bg-line md:grid-cols-3">
      {problems.map((problem, index) => (
        <li class="bg-ground px-6 py-9 sm:px-8">
          <span class="font-display text-sm font-semibold uppercase tracking-[0.14em] text-accent">
            {String(index + 1).padStart(2, '0')}
          </span>
          <h3 class="display-3 mt-2">{problem.title}</h3>
          <p class="mt-3 leading-relaxed text-muted">{problem.description}</p>
        </li>
      ))}
    </ol>

    <p class="display-3 mt-14 max-w-3xl border-l-2 border-accent-fill pl-6">
      {problemResolution}
    </p>
  </div>
</section>
